# pg-aje-by-yhw
pg-aje — A pure PL/pgSQL extension for PostgreSQL 18 that maps relational tables to updatable JSON document views with composite PK support, nested arrays, annotation-based access control, and ETAG optimistic concurrency.
